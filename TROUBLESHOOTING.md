# 🐛 دليل استكشاف الأخطاء والمشاكل

## الأخطاء الشائعة والحلول

---

## 1. ❌ خطأ "Port 3000 is already in use"

### الأعراض:
```
Error: listen EADDRINUSE: address already in use :::3000
```

### الحلول:
```bash
# على Windows - البحث عن العملية التي تستخدم المنفذ
netstat -ano | findstr :3000

# إيقاف العملية (استبدل PID برقم العملية)
taskkill /PID <PID> /F

# أو استخدام منفذ مختلف
PORT=3001 npm start
```

```bash
# على macOS/Linux
lsof -i :3000
kill -9 <PID>
```

---

## 2. ❌ خطأ "Cannot find module '@prisma/client'"

### الأعراض:
```
Error: Cannot find module '@prisma/client'
```

### الحلول:
```bash
# أعد تثبيت المكتبات
npm install

# وليد عميل Prisma
npx prisma generate

# أعد محاولة التشغيل
npm start
```

---

## 3. ❌ خطأ قاعدة البيانات

### الأعراض:
```
PrismaClientInitializationError: Can't reach database server
```

### الحلول:
```bash
# تحقق من المسار في .env
# يجب أن يكون: DATABASE_URL="file:./prisma/dev.db"

# أعد إنشاء قاعدة البيانات
npx prisma migrate dev --name init

# أو احذفها وأعد إنشاءها
rm prisma/dev.db
npx prisma migrate dev --name init
node prisma/seed.js
```

---

## 4. ❌ خطأ إرسال البريد الإلكتروني

### الأعراض:
```
Error: Invalid login: 535-5.7.8 Username and password not accepted
```

### الحلول:
1. **تحقق من App Password:**
   - اذهب إلى https://myaccount.google.com/security
   - فعّل "2-Step Verification" إذا لم يكن مفعلاً
   - اذهب إلى App Passwords
   - اختر "Mail" و "Windows Computer"
   - انسخ كلمة المرور المُنشأة

2. **حدّث ملف .env:**
```env
GMAIL_USER=your-email@gmail.com
GMAIL_PASSWORD=your-16-character-app-password
```

3. **أعد تشغيل الخادم:**
```bash
npm start
```

---

## 5. ❌ خطأ تسجيل الدخول للإدارة

### الأعراض:
```
Invalid login credentials
```

### الحلول:
```bash
# تحقق من وجود حساب الإدارة
npx prisma studio

# ابحث عن جدول Admin وتحقق من البيانات

# أعد تشغيل البذر لإنشاء حساب جديد
node prisma/seed.js
```

البيانات الافتراضية:
- البريد: `admin@qareebeen.com`
- كلمة المرور: `admin123`

---

## 6. ❌ خطأ "CORS error" عند الطلبات

### الأعراض:
```
Access to XMLHttpRequest at 'http://localhost:3000/api/submit'
from origin 'http://localhost:3000' has been blocked by CORS policy
```

### الحلول:
```bash
# تحقق من تثبيت cors package
npm install cors

# تأكد أن app.js يحتوي على:
app.use(cors());
```

---

## 7. ❌ الملفات الثابتة (CSS, JS) لا تحمّل

### الأعراض:
- الصفحات تظهر بدون تنسيق
- السكريبتات لا تعمل

### الحلول:
```bash
# تحقق من وجود الملفات في public/
ls public/

# يجب أن تكون موجودة:
# - style.css
# - admin-style.css
# - script.js
# - admin-script.js

# إذا لم تكن موجودة، انسخها:
cp style.css public/style.css
cp admin-style.css public/admin-style.css
```

---

## 8. ❌ قاعدة البيانات مقفلة (Database Locked)

### الأعراض:
```
Error: database is locked
```

### الحلول:
```bash
# أغلق جميع اتصالات Prisma Studio
# أغلق جميع جلسات npm

# اقتل العملية وأعد التشغيل
npm start

# إذا استمرت المشكلة، احذف قفل قاعدة البيانات:
rm prisma/dev.db-shm
rm prisma/dev.db-wal
```

---

## 9. ❌ النموذج لا يُرسل

### الأعراض:
- النموذج لا يرسل عند النقر على الزر
- أو يُرسل بدون رسالة نجاح

### الحلول:
```bash
# افتح أدوات المطور (F12)
# انظر إلى قسم Console للأخطاء

# تحقق من:
1. أن جميع الحقول مملوءة
2. أن البريد الإلكتروني صحيح
3. أن الاتصال بالخادم يعمل

# جرب هذا في Console:
fetch('/api/submit', {
    method: 'POST',
    headers: {'Content-Type': 'application/json'},
    body: JSON.stringify({
        studentName: 'اختبار',
        studentId: '123',
        faculty: 'engineering',
        year: '1',
        email: 'test@example.com',
        type: 'suggestion',
        title: 'اختبار',
        content: 'اختبار المشاركة',
        university: 'government'
    })
}).then(r => r.json()).then(d => console.log(d))
```

---

## 10. ❌ عدم رؤية المشاركات في لوحة التحكم

### الأعراض:
- صفحة "لا توجد مشاركات" حتى بعد إرسال مشاركة

### الحلول:
```bash
# تحقق من قاعدة البيانات
npx prisma studio

# ابحث عن جدول Submission وتحقق من البيانات

# إذا كانت فارغة:
1. جرب إرسال مشاركة جديدة
2. تحقق من عدم وجود أخطاء في الخادم

# إذا كانت موجودة لكن لا تظهر:
# قد تكون مشكلة في الـ JavaScript
# افتح أدوات المطور وتحقق من الأخطاء
```

---

## 11. ❌ الرسوم البيانية لا تظهر

### الأعراض:
- صفحة التحليلات فارغة أو بها رسوم بيانية فارغة

### الحلول:
```bash
# تحقق من تحميل Chart.js
# افتح أدوات المطور > Network > jsd/Chart.js

# إذا لم تحمّل:
# تحقق من الاتصال بالإنترنت
# أو حمّل Chart.js محلياً:
npm install chart.js

# أضف إلى analytics.ejs:
<script src="/path/to/chart.js"></script>

# تأكد من أن هناك بيانات في قاعدة البيانات
```

---

## 12. ❌ رسالة البريد الإلكتروني لم تصل

### الأعراض:
- المستخدم أرسل مشاركة لكن لم يستقبل رسالة تأكيد

### الحلول:
```bash
# تحقق من:
1. البريد الخاص للرسائل غير المرغوبة (Spam)
2. أن البريد الإلكتروني المُدخل صحيح
3. أن بيانات Gmail صحيحة في .env

# شغّل الخادم بوضع debug:
NODE_DEBUG=* npm start

# تحقق من الأخطاء في الخادم
```

---

## 13. ❌ الصفحات تعطي 404 Not Found

### الأعراض:
```
Cannot GET /government
```

### الحلول:
```bash
# تحقق من أن الخادم يعمل:
http://localhost:3000

# إذا لم يعمل، شغّل:
npm start

# تأكد من أن الـ Routes في app.js صحيحة:
app.get('/government', (req, res) => {
    res.render('government', { university: 'government' });
});
```

---

## 14. ❌ الجلسة تنتهي بسرعة

### الأعراض:
- تسجيل دخول الإدارة ثم إعادة تحويل إلى صفحة التسجيل

### الحلول:
```bash
# تحقق من SESSION_SECRET في .env
# تأكد أنه موجود ومختلف عن القيمة الافتراضية

# أعد تشغيل الخادم:
npm start

# إذا استمرت المشكلة، جرب:
# احذف cookies من المتصفح
# وسجل دخولك مجدداً
```

---

## 15. ❌ رسالة "Too many connections"

### الأعراض:
```
Error: too many connections
```

### الحلول:
```bash
# أغلق جميع اتصالات قاعدة البيانات
# من خلال إغلاق جميع جلسات Prisma Studio

# أضف هذا إلى app.js:
process.on('SIGINT', async () => {
    await prisma.$disconnect();
    process.exit(0);
});

# أعد التشغيل:
npm start
```

---

## 🔧 نصائح عامة لاستكشاف الأخطاء

### استخدم أدوات المطور:
```
1. F12 - فتح أدوات المطور
2. Console - لرؤية الأخطاء
3. Network - لرؤية الطلبات
4. Application > Cookies - للتحقق من الجلسات
```

### تحقق من السجلات:
```bash
# شاهد أخطاء الخادم في الـ Terminal
npm start

# ستظهر جميع الأخطاء والطلبات هناك
```

### استخدم Prisma Studio:
```bash
# لعرض قاعدة البيانات بصرياً
npx prisma studio
```

### جرّب API مباشرة:
```bash
# استخدم curl أو Postman
curl -X GET http://localhost:3000/api/submissions
```

---

## 📞 إذا استمرت المشكلة

1. **تحقق من المتطلبات:**
   - Node.js v16+
   - npm حديث

2. **أعد التثبيت من الصفر:**
   ```bash
   rm -rf node_modules package-lock.json
   npm install
   npm run setup
   ```

3. **اتصل بالدعم:**
   - 📧 omarwassim05@gmail.com
   - 🐙 GitHub Issues

---

**نتمنى لك تجربة ممتعة مع قريبين! 💚**
