# قريبين - منصة الاقتراحات والاستفسارات 🎓

نظام متكامل لاستقبال واستدارة الاقتراحات والاستفسارات من طلاب جامعات حلوان الثلاث (الحكومية، الأهلية، التكنولوجية).

---

## ✨ المميزات الرئيسية

- ✅ **واجهة مستخدم احترافية** - تصميم عصري وسهل الاستخدام
- 📧 **إرسال بريد إلكتروني تلقائي** - إشعارات للمستخدمين والإدارة
- 📊 **لوحة تحليلات متقدمة** - رسوم بيانية وإحصائيات تفصيلية
- 🛡️ **لوحة تحكم آمنة** - نظام تسجيل دخول وإدارة مشاركات
- 🔍 **بحث وتصفية متقدمة** - فلترة المشاركات حسب معايير مختلفة
- 💾 **قاعدة بيانات محمية** - Prisma مع SQLite
- 📱 **تصميم متجاوب** - يعمل على جميع الأجهزة

---

## 🛠️ متطلبات التثبيت

- Node.js v16.0.0 أو أحدث
- npm أو yarn
- بريد Gmail مع App Password

---

## 📦 خطوات التثبيت والتشغيل

### 1. استنساخ المستودع
```bash
git clone https://github.com/YossefAhmed2025/HNU-suggestion-platform.git
cd HNU-suggestion-platform/reports
```

### 2. تثبيت المكتبات
```bash
npm install
```

### 3. تكوين المتغيرات البيئية
قم بنسخ ملف `.env` وأضف بيانات بريدك Gmail:

```env
# Database URL
DATABASE_URL="file:./prisma/dev.db"

# Email Configuration
GMAIL_USER="your-email@gmail.com"
GMAIL_PASSWORD="your-app-password"
ADMIN_EMAIL="admin@gmail.com"

# Server Configuration
PORT=3000
NODE_ENV="development"

# Session Secret
SESSION_SECRET="your-secret-key-change-this-in-production"
```

### 4. إنشاء قاعدة البيانات والجداول
```bash
npx prisma migrate dev --name init
```

### 5. إنشاء حساب الإدارة الافتراضي
```bash
node prisma/seed.js
```

### 6. تشغيل الخادم
```bash
npm start
```

أو للتطوير مع إعادة تحميل تلقائية:
```bash
npm run dev
```

---

## 🌐 الوصول إلى التطبيق

- **الصفحة الرئيسية:** http://localhost:3000
- **لوحة التحكم:** http://localhost:3000/admin
- **لوحة التحليلات:** http://localhost:3000/analytics

---

## 🔐 بيانات الدخول الافتراضية

- **البريد الإلكتروني:** admin@qareebeen.com
- **كلمة المرور:** admin123

⚠️ **تحذير:** غير كلمة المرور الافتراضية فوراً قبل نشر التطبيق على الإنترنت

---

## 📧 إعدادات البريد الإلكتروني

### للبريد الإلكتروني المجاني (Gmail)

1. انتقل إلى حسابك على Google
2. اذهب إلى [Security Settings](https://myaccount.google.com/security)
3. فعّل المصادقة متعددة العوامل (2-Step Verification)
4. انتقل إلى قسم App Passwords
5. أنشئ كلمة مرور تطبيق لـ Gmail
6. استخدم كلمة المرور المُنشأة في ملف `.env`

---

## 🎨 الهيكل والملفات

```
HNU-suggestion-platform/reports/
├── app.js                    # الملف الرئيسي للخادم
├── package.json             # المكتبات والمتغيرات
├── .env                      # المتغيرات البيئية
├── prisma/
│   ├── schema.prisma        # نموذج قاعدة البيانات
│   ├── seed.js              # بيانات البداية
│   └── dev.db               # قاعدة البيانات (SQLite)
├── views/                    # قوالب EJS
│   ├── index.ejs            # الصفحة الرئيسية
│   ├── government.ejs       # نموذج الجامعة الحكومية
│   ├── private.ejs          # نموذج الجامعة الأهلية
│   ├── tech.ejs             # نموذج الجامعة التكنولوجية
│   ├── admin-login.ejs      # صفحة تسجيل دخول الإدارة
│   ├── admin.ejs            # لوحة تحكم الإدارة
│   └── analytics.ejs        # لوحة التحليلات
├── public/                   # الملفات الثابتة
│   ├── style.css            # الأنماط الرئيسية
│   ├── admin-style.css      # أنماط لوحة التحكم
│   ├── script.js            # البرامج النصية من جانب العميل
│   └── admin-script.js      # برامج لوحة التحكم
└── README.md                # هذا الملف
```

---

## 📊 نموذج قاعدة البيانات

### جدول Submission (المشاركات)
- `id` - معرف فريد
- `studentName` - اسم الطالب
- `studentId` - رقم الطالب
- `faculty` - الكلية
- `year` - السنة الدراسية
- `email` - البريد الإلكتروني
- `type` - نوع المشاركة (اقتراح/استفسار)
- `title` - عنوان المشاركة
- `content` - محتوى المشاركة
- `university` - الجامعة
- `status` - حالة المشاركة
- `adminNotes` - ملاحظات الإدارة
- `createdAt` - تاريخ الإنشاء
- `updatedAt` - تاريخ التحديث

### جدول Admin (حسابات الإدارة)
- `id` - معرف فريد
- `email` - البريد الإلكتروني
- `password` - كلمة المرور (مشفرة)
- `name` - الاسم

### جدول Analytics (الإحصائيات)
- `id` - معرف فريد
- `date` - التاريخ
- `totalSubmissions` - إجمالي المشاركات
- `suggestionsCount` - عدد الاقتراحات
- `inquiriesCount` - عدد الاستفسارات
- وعدادات أخرى...

---

## 🔄 المسارات الرئيسية (Routes)

### الصفحات العامة
- `GET /` - الصفحة الرئيسية
- `GET /government` - صفحة الجامعة الحكومية
- `GET /private` - صفحة الجامعة الأهلية
- `GET /tech` - صفحة الجامعة التكنولوجية

### APIs وسائط النقل
- `POST /api/submit` - إرسال مشاركة جديدة
- `GET /api/submissions` - الحصول على المشاركات (يتطلب تسجيل دخول)
- `GET /api/submissions/:id` - الحصول على تفاصيل مشاركة
- `PUT /api/submissions/:id` - تحديث المشاركة
- `DELETE /api/submissions/:id` - حذف المشاركة
- `GET /api/analytics` - الحصول على البيانات الإحصائية

### لوحة التحكم
- `GET /admin/login` - صفحة تسجيل الدخول
- `POST /admin/login` - معالج تسجيل الدخول
- `GET /admin` - لوحة التحكم الرئيسية
- `GET /analytics` - لوحة التحليلات
- `GET /logout` - تسجيل الخروج

---

## 🐛 استكشاف الأخطاء

### المشكلة: خطأ "Cannot find module '@prisma/client'"
**الحل:** قم بتشغيل `npm install` ثم `npx prisma generate`

### المشكلة: خطأ في إرسال البريد الإلكتروني
**الحل:** 
- تأكد من صحة بيانات Gmail في ملف `.env`
- تأكد من تفعيل "App Passwords" في حساب Google
- تحقق من أن `GMAIL_PASSWORD` هو كلمة مرور التطبيق وليس كلمة المرور الرئيسية

### المشكلة: قاعدة البيانات لم تُنشأ
**الحل:** قم بتشغيل `npx prisma migrate dev --name init`

### المشكلة: الوصول لـ /admin يعيد تحويل إلى /admin/login
**الحل:** تحقق من أن تسجيل الدخول نجح وأن الـ session حُفظ بشكل صحيح

---

## 📝 أمثلة الاستخدام

### إرسال مشاركة جديدة
```javascript
fetch('/api/submit', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
        studentName: 'أحمد محمد',
        studentId: '2020001',
        faculty: 'engineering',
        year: '3',
        email: 'ahmed@student.helwan.edu.eg',
        type: 'suggestion',
        title: 'تحسين المكتبة',
        content: 'اقترح تحسين أوقات عمل المكتبة...',
        university: 'government'
    })
})
```

### الحصول على المشاركات
```javascript
fetch('/api/submissions?university=government&type=suggestion&status=pending')
    .then(res => res.json())
    .then(data => console.log(data))
```

---

## 🚀 نشر التطبيق على الإنترنت

### استخدام Heroku
1. قم بإنشاء حساب على [Heroku](https://www.heroku.com)
2. تثبيت Heroku CLI
3. `heroku login`
4. `heroku create your-app-name`
5. `git push heroku main`

### متطلبات الإنتاج
- استخدم قاعدة بيانات PostgreSQL أو MySQL بدلاً من SQLite
- عدّل `DATABASE_URL` في البيئة الإنتاجية
- عدّل `NODE_ENV` إلى "production"
- عيّن `SESSION_SECRET` إلى قيمة قوية عشوائية
- فعّل HTTPS و الأمان الكامل

---

## 📞 التواصل والدعم

إذا واجهت أي مشاكل أو لديك اقتراحات:
- 📧 البريد الإلكتروني: omarwassim05@gmail.com
- 🐙 GitHub: [YossefAhmed2025](https://github.com/YossefAhmed2025)

---

## 📄 الترخيص

هذا المشروع مرخص تحت MIT License - انظر ملف LICENSE للتفاصيل.

---

## 👨‍💻 الفريق

تم تطوير هذا المشروع بواسطة:
- **Youssef Ahmed** - المطور الرئيسي

---

## 🎯 نسخة المشروع
**الإصدار:** 1.0.0  
**آخر تحديث:** ديسمبر 2025

---

**شكراً لاستخدامك قريبين! 💚**
